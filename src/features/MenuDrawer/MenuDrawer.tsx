import React from "react";
import { Drawer, DrawerHeader, DrawerPrimaryMenu } from "./components";

export const MenuDrawer = () => {
  return (
    <Drawer>
      <DrawerHeader />
      {/* TODO: add DrawerMenu */}
      <DrawerPrimaryMenu />
    </Drawer>
  );
};
